#pragma once

#include <cstdint>
#include <cstddef>

namespace DynamicMemory
{
    namespace Options
    {
        /* Length of input & output circular buffers. */
        const size_t MemorySize     = 1024 * 1024;
    }
}
